import { money } from '../dashboard.utils'
export default function DashboardLists({ topProducts = [], recentSales = [] }) { return <div className="dashboard-list-grid"><section className="dashboard-card"><header><h2>Top Products by Sales</h2></header><div className="rank-list">{topProducts.length ? topProducts.map((product, index) => <div key={product.id || `${product.name}-${index}`}><b>{index + 1}</b><div><strong>{product.name}</strong>{product.brand_name && <small>{product.brand_name}</small>}</div><span>{product.qty_sold} sold</span><div className="rank-money"><strong>Rs. {money(product.revenue)}</strong><small>COGS: Rs. {money(product.cogs)}</small></div></div>) : <p className="dashboard-empty">No sales in this period.</p>}</div></section><section className="dashboard-card"><header><h2>Recent Sales</h2></header><div className="recent-list">{recentSales.length ? recentSales.map((sale) => <div key={sale.id}><span>{new Date(sale.created_at).toLocaleString()}</span><strong>Rs. {money(sale.total)}</strong></div>) : <p className="dashboard-empty">No sales in this period.</p>}</div></section></div> }
