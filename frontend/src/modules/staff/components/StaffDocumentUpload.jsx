import {useState} from 'react';
export default function StaffDocumentUpload({staff,categories,onUploaded}){const [form,setForm]=useState({staff_profile_id:'',category_id:'',title:'',issued_on:'',expires_on:'',retention_until:'',notes:''}),[file,setFile]=useState(null),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{const body=new FormData();Object.entries(form).forEach(([k,v])=>v&&body.append(k,v));body.append('document',file);const response=await fetch('/api/documents',{method:'POST',credentials:'include',headers:{Accept:'application/json'},body});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Upload failed.');setForm(v=>({...v,title:'',issued_on:'',expires_on:'',retention_until:'',notes:''}));setFile(null);onUploaded()}catch(requestError){setError(requestError.message)}finally{setBusy(false)}}return <form className="document-upload" onSubmit={submit}><h3>Upload private document</h3>{error&&<div className="form-error">{error}</div>}<div className="form-row"><label>Staff<select required value={form.staff_profile_id} onChange={e=>setForm({...form,staff_profile_id:e.target.value})}><option value="">Select staff</option>{staff.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label>Category<select required value={form.category_id} onChange={e=>setForm({...form,category_id:e.target.value})}><option value="">Select category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label></div><label>Title<input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>PDF or image (maximum 10 MB)<input required type="file" accept="application/pdf,image/jpeg,image/png,image/webp" onChange={e=>setFile(e.target.files[0]||null)}/></label><div className="form-row"><label>Issued on<input type="date" value={form.issued_on} onChange={e=>setForm({...form,issued_on:e.target.value})}/></label><label>Expires on<input type="date" value={form.expires_on} onChange={e=>setForm({...form,expires_on:e.target.value})}/></label><label>Retain until<input type="date" value={form.retention_until} onChange={e=>setForm({...form,retention_until:e.target.value})}/></label></div><label>Notes<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><button className="primary-button" disabled={busy||!file}>{busy?'Uploading…':'Upload securely'}</button></form>}
