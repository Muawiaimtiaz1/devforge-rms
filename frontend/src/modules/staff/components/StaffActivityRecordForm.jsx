import {useState} from 'react';import {api} from '../../../api/client';
const today=()=>new Date().toISOString().slice(0,10);export default function StaffActivityRecordForm({staff,onCreated}){const[form,setForm]=useState({staff_profile_id:'',record_type:'task',title:'',details:'',occurred_on:today(),due_date:'',severity:'normal',is_private:false}),[error,setError]=useState('');async function submit(e){e.preventDefault();try{await api('/api/staff-activity/records',{method:'POST',body:{...form,due_date:form.record_type==='task'?form.due_date:'',severity:form.severity||null}});setForm(v=>({...v,title:'',details:'',due_date:''}));onCreated()}catch(requestError){setError(requestError.message)}}return <form className="activity-record-form" onSubmit={submit}><h3>Add task or staff record</h3>{error&&<div className="form-error">{error}</div>}<div className="form-row"><label>Staff<select required value={form.staff_profile_id} onChange={e=>setForm({...form,staff_profile_id:e.target.value})}><option value="">Select staff</option>{staff.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label>Type<select value={form.record_type} onChange={e=>setForm({...form,record_type:e.target.value})}><option value="task">Assigned task</option><option value="operational_note">Operational note</option><option value="recognition">Recognition</option><option value="disciplinary">Disciplinary record</option></select></label><label>Date<input type="date" value={form.occurred_on} onChange={e=>setForm({...form,occurred_on:e.target.value})}/></label></div><label>Title<input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Details<textarea required value={form.details} onChange={e=>setForm({...form,details:e.target.value})}/></label><div className="form-row">{form.record_type==='task'&&<label>Due date<input type="date" value={form.due_date} onChange={e=>setForm({...form,due_date:e.target.value})}/></label>}<label>Priority / severity<select value={form.severity} onChange={e=>setForm({...form,severity:e.target.value})}><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option><option value="critical">Critical</option></select></label><label className="check-label"><input type="checkbox" checked={form.is_private} onChange={e=>setForm({...form,is_private:e.target.checked})}/> Manager-private record</label></div><button className="primary-button">Save record</button></form>}
