export default function StaffActivityFilters({filters,options,onChange,canExport}){const query=new URLSearchParams(Object.fromEntries(Object.entries(filters).filter(([,v])=>v&&v!=='all'))).toString();return <div className="activity-filters"><label>From<input type="date" value={filters.from} onChange={e=>onChange('from',e.target.value)}/></label><label>To<input type="date" value={filters.to} onChange={e=>onChange('to',e.target.value)}/></label><label>Department<select value={filters.department_id} onChange={e=>onChange('department_id',e.target.value)}><option value="">All departments</option>{options.departments?.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select></label><label>Staff<select value={filters.staff_profile_id} onChange={e=>onChange('staff_profile_id',e.target.value)}><option value="">All accessible staff</option>{options.staff?.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label>{canExport&&<a className="secondary-button" href={`/api/staff-activity/export?${query}`}>Export CSV</a>}</div>}
