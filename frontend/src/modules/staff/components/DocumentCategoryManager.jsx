import {useState} from 'react';import {api} from '../../../api/client';
export default function DocumentCategoryManager({onChanged}){const [form,setForm]=useState({name:'',description:'',default_retention_days:'',requires_expiry:false}),[error,setError]=useState('');async function submit(e){e.preventDefault();try{await api('/api/documents/categories',{method:'POST',body:form});setForm({name:'',description:'',default_retention_days:'',requires_expiry:false});onChanged()}catch(requestError){setError(requestError.message)}}return <form className="document-category-form" onSubmit={submit}><h3>Document category</h3>{error&&<div className="form-error">{error}</div>}<div className="form-row"><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Default retention days<input type="number" min="0" value={form.default_retention_days} onChange={e=>setForm({...form,default_retention_days:e.target.value})}/></label></div><label>Description<input value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label className="check-label"><input type="checkbox" checked={form.requires_expiry} onChange={e=>setForm({...form,requires_expiry:e.target.checked})}/> Require an expiry date</label><button className="secondary-button">Add category</button></form>}
