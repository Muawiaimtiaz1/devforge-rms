const service=require('./documents.service');function meta(req){return{ip:req.ip,userAgent:String(req.get('user-agent')||'').slice(0,500)}}
async function categories(req,res){res.json(await service.categories(req.session.user))}async function staffOptions(req,res){res.json(await service.staffOptions(req.session.user))}async function createCategory(req,res){res.status(201).json(await service.createCategory(req.session.user,req.body))}async function list(req,res){res.json(await service.list(req.session.user,req.query))}async function upload(req,res){res.status(201).json(await service.upload(req.session.user,req.body,req.file,meta(req)))}
async function serve(req,res,action){const{doc,buffer}=await service.getFile(req.session.user,req.params.id,action,meta(req));res.set({'Cache-Control':'no-store, private','X-Content-Type-Options':'nosniff','Content-Type':doc.mime_type,'Content-Length':buffer.length});if(action==='download')res.attachment(doc.original_name);else res.setHeader('Content-Disposition',`inline; filename="document-${doc.id}"`);res.send(buffer)}async function view(req,res){return serve(req,res,'view')}async function download(req,res){return serve(req,res,'download')}async function archive(req,res){res.json(await service.archive(req.session.user,req.params.id,meta(req)))}module.exports={categories,staffOptions,createCategory,list,upload,view,download,archive};
