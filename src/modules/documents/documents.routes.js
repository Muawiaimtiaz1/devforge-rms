const express=require('express'),multer=require('multer');const {requirePermission}=require('../../../authorization/middleware'),controller=require('./documents.controller'),router=express.Router();
const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:10*1024*1024,files:1,fields:10},fileFilter:(req,file,cb)=>cb(null,['application/pdf','image/jpeg','image/png','image/webp'].includes(file.mimetype))});
router.get('/categories',requirePermission('documents.view'),controller.categories);router.get('/staff-options',requirePermission('documents.view'),controller.staffOptions);router.post('/categories',requirePermission('documents.manage'),controller.createCategory);router.get('/',requirePermission('documents.view'),controller.list);router.post('/',requirePermission('documents.upload'),upload.single('document'),controller.upload);router.get('/:id/view',requirePermission('documents.view'),controller.view);router.get('/:id/download',requirePermission('documents.download'),controller.download);router.patch('/:id/archive',requirePermission('documents.manage'),controller.archive);module.exports=router;
