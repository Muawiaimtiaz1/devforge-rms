const express=require('express'),{requirePermission}=require('../../../authorization/middleware'),controller=require('./staff-activity.controller'),router=express.Router();router.get('/filters',requirePermission('staff_activity.view'),controller.filters);router.get('/report',requirePermission('staff_activity.view'),controller.report);router.get('/records',requirePermission('staff_activity.view'),controller.records);router.post('/records',requirePermission('staff_activity.manage'),controller.create);router.patch('/records/:id/status',requirePermission('staff_activity.manage'),controller.status);router.get('/export',requirePermission('staff_activity.export'),controller.exportReport);module.exports=router;
