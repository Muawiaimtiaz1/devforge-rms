require('dotenv').config({quiet:true});const db=require('../db/knex'),repository=require('../src/modules/staff-activity/staff-activity.repository');async function main(){const shop=await db('shops').whereNotNull('id').orderBy('id').first('id');if(!shop){console.log(JSON.stringify({checked:true,shop:null,rows:0}));return}const to=new Date().toISOString().slice(0,10),from=new Date(Date.now()-30*86400000).toISOString().slice(0,10);const rows=await repository.report(shop.id,{from,to},null);console.log(JSON.stringify({checked:true,shop:shop.id,rows:rows.length,sampleColumns:rows[0]?Object.keys(rows[0]):[]}))}main().catch(e=>{console.error('Staff activity check failed:',e);process.exitCode=1}).finally(()=>db.destroy());
