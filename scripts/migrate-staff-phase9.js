require('dotenv').config({quiet:true});const{query,close}=require('../db/postgres'),{ensureStaffActivitySchema}=require('../src/modules/staff-activity/staff-activity.migration');async function main(){await ensureStaffActivitySchema(query);const result=await query("SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name LIKE 'staff_activity%' ORDER BY table_name");console.log(JSON.stringify({migrated:true,tables:result.rows.map(r=>r.table_name)}))}main().catch(e=>{console.error('Staff activity migration failed:',e);process.exitCode=1}).finally(close);
