require('dotenv').config({quiet:true}); const {query,close}=require('../db/postgres'); const {ensureAttendanceSchema}=require('../src/modules/attendance/attendance.migration'); const {ensurePayrollSchema}=require('../src/modules/payroll/payroll.migration');
async function main(){await ensureAttendanceSchema(query);await ensurePayrollSchema(query);const result=await query("SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND (table_name LIKE 'payroll_%' OR table_name LIKE 'attendance_summary_snapshot%') ORDER BY table_name");console.log(JSON.stringify({migrated:true,tables:result.rows.map(r=>r.table_name)}));}main().catch(e=>{console.error('Payroll migration failed:',e);process.exitCode=1;}).finally(close);
