const test=require('node:test'),assert=require('node:assert/strict');const{KnexSessionStore}=require('../server');
test('database session store rejects and deletes an expired session',async()=>{let deleted=false;const row={sid:'expired',sess:JSON.stringify({user:{id:1}}),expires:new Date(Date.now()-1000).toISOString()};const knex=()=>({where(){return this},first(){return Promise.resolve(row)},del(){deleted=true;return Promise.resolve(1)}});const store=new KnexSessionStore(knex);store.ensureReady=async()=>{};const value=await new Promise((resolve,reject)=>store.get('expired',(error,session)=>error?reject(error):resolve(session)));assert.equal(value,null);assert.equal(deleted,true)});
