const test=require('node:test'),assert=require('node:assert/strict');const{app}=require('../server');
test('unauthenticated Staff APIs deny access and send private no-store headers',async()=>{const server=app.listen(0);try{const{port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/api/staff`);assert.equal(response.status,401);assert.match(response.headers.get('cache-control'),/no-store/);assert.equal(response.headers.get('x-content-type-options'),'nosniff');assert.equal(response.headers.get('x-frame-options'),'SAMEORIGIN')}finally{await new Promise(resolve=>server.close(resolve))}});
